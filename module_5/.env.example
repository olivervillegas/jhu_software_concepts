# Database connection (least privilege user recommended)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gradcafe
DB_USER=gradcafe_app
DB_PASSWORD=CHANGEME

# Optional override (use only if instructed; never commit secrets)
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# Flask
FLASK_DEBUG=0
